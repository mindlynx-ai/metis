# @mindlynx/metis-core

The control-plane HTTP surface: login + bearer sessions, workflow CRUD + publish, execution start/read/signal/cancel, connections with the write-only credential boundary, data-resource routes (tables/validate) and catalogue serving.

See [docs/architecture.md](../../docs/architecture.md) for how the packages fit together.
