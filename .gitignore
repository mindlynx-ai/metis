node_modules/
dist/
coverage/
.metis/
*.local.md
.env
test-results/
playwright-report/

# AWS demo box: local-only secrets, state and private key
deploy/aws*/.env
deploy/aws*/.state
deploy/aws*/*.pem

# Internal planning/porting notes: never ship in the public repo.
docs/internal/

# MkDocs build output (docs.metisflow.io is built in CI)
site/

# OS noise
.DS_Store

# Playwright visual baselines are LOCAL: platform-suffixed (-darwin/-linux),
# validated by no CI job, regenerated on first run. Never ship them.
*-snapshots/
