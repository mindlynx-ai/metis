# @mindlynx/metis-orchestrator

The Temporal client side: the execution adapter (start, signal, cooperative cancel, describe, list), the schedule service, trigger services (webhook/poll/event), outbound webhooks, and the Socket.IO hub that streams engine events to the editor.

See [docs/architecture.md](../../docs/architecture.md) for how the packages fit together.
