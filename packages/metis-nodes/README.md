# @mindlynx/metis-nodes

The open node handlers: HTTP/api requests, the sandboxed code node, the Data node's Postgres adapter (DataSourcePort), SendGrid, and the generated connector handlers. Handlers receive already-substituted config and return one payload.

See [docs/architecture.md](../../docs/architecture.md) for how the packages fit together.
