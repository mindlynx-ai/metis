# @mindlynx/metis-editor

The React app: the React Flow canvas, schema-driven inspectors (with per-node Guide docs), the picker, runs + history pages with live WebSocket updates. Talks only to metis-core's HTTP surface.

See [docs/architecture.md](../../docs/architecture.md) for how the packages fit together.
